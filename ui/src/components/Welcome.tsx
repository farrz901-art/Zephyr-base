export function Welcome() {
  return (
    <section className="hero-card">
      <p className="eyebrow">Zephyr Base</p>
      <h1>Local-only desktop processing shell</h1>
      <p className="hero-copy">
        This first UI slice consumes bundled adapter artifacts for local text and markdown-class
        inputs only. It does not claim installer-complete runtime packaging yet.
      </p>
    </section>
  );
}
